# Same origin as Next — browser calls this; Next proxies to Nest (API_PROXY_TARGET)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
# Server-side rewrite target (Nest). Not sent to the client.
API_PROXY_TARGET=http://127.0.0.1:3001
